# Changelog

All notable changes to this component are documented here.

## [0.1.0] - 2026-07-30

### Added

- **provider**: add the Terraform provider and infra_vpc resource (`55d913a`)
- **provider**: authenticate with a bearer token (`8c34696`)
- **provider**: add a generic resource core (`2c8c86a`)
- **provider**: add network and compute Terraform resources (`74f5a3b`)
- **provider**: add DNS, peering, load-balancer, WAF and node Terraform resources (`876b5be`)
- **core**: add compute node-pool placement (`0e1c993`)

### Dependencies

- Track `infra-api` `0.1.0`
