# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Chris <goabonga@pm.me>

apiVersion: v2
name: infra
description: >-
  Declarative Linux-native cloud control plane: API server, controller-manager,
  identity provider, Prometheus exporter and web dashboard.
type: application
# Chart version. Bumped by multicz as the `infra-chart` component, including
# the patch it cascades whenever a component image version is mirrored into
# values.yaml.
version: 0.0.1
# appVersion tracks the API server, which is the component that defines the
# resource API the rest of the chart deploys against.
appVersion: "0.1.0"
home: https://goabonga.github.io/infrastructure/
sources:
  - https://github.com/goabonga/infrastructure
maintainers:
  - name: Chris
    email: goabonga@pm.me
keywords:
  - infrastructure
  - control-plane
  - declarative
