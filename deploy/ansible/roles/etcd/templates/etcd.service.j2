# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Chris <goabonga@pm.me>
[Unit]
Description=etcd key-value store (infra shared state backend)
After=network-online.target
Wants=network-online.target

[Service]
User=etcd
Group=etcd
Type=notify
ExecStart=/usr/local/bin/etcd \
  --name {{ control_host }} \
  --data-dir {{ etcd_data_dir }} \
  --listen-client-urls http://0.0.0.0:{{ etcd_client_port }} \
  --advertise-client-urls http://{{ control_ip }}:{{ etcd_client_port }} \
  --listen-peer-urls http://{{ control_ip }}:{{ etcd_peer_port }} \
  --initial-advertise-peer-urls http://{{ control_ip }}:{{ etcd_peer_port }} \
  --initial-cluster {{ control_host }}=http://{{ control_ip }}:{{ etcd_peer_port }} \
  --initial-cluster-state new \
  --initial-cluster-token infra-etcd
Restart=on-failure
RestartSec=5
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
