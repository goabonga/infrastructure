#cloud-config
hostname: {{ item.name }}
fqdn: {{ item.name }}.local
manage_etc_hosts: true
users:
  - name: {{ vm_user }}
    sudo: "ALL=(ALL) NOPASSWD:ALL"
    groups: [sudo]
    shell: /bin/bash
    ssh_authorized_keys:
      - {{ ssh_public_key }}
ssh_pwauth: false
package_update: true
